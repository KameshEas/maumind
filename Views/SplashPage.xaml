<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauMind.App"
             x:Class="MauMind.App.Views.SplashPage"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.HasBackButton="False">

    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        
        <!-- Clean Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}"
                              Offset="0" />
                <GradientStop Color="{AppThemeBinding Light={StaticResource LightBackgroundGradientEnd}, Dark={StaticResource DarkBackgroundGradientEnd}}"
                              Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Decorative Elements -->
        <Ellipse WidthRequest="300" HeightRequest="300"
                 HorizontalOptions="Start" VerticalOptions="Start"
                 Margin="-100,-100,0,0"
                 Opacity="0.05">
            <Ellipse.Fill>
                <SolidColorBrush Color="{StaticResource Primary}"/>
            </Ellipse.Fill>
        </Ellipse>

        <Ellipse WidthRequest="200" HeightRequest="200"
                 HorizontalOptions="End" VerticalOptions="End"
                 Margin="0,0,-60,-60"
                 Opacity="0.04">
            <Ellipse.Fill>
                <SolidColorBrush Color="{StaticResource Accent}"/>
            </Ellipse.Fill>
        </Ellipse>

        <VerticalStackLayout HorizontalOptions="Center" 
                            VerticalOptions="Center"
                            Spacing="20">
            
            <!-- Logo Container -->
            <Grid HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="100" HeightRequest="100">
                
                <!-- Subtle Glow (pulsing) -->
                <Ellipse x:Name="PulsingCircle" WidthRequest="80" 
                         HeightRequest="80"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Opacity="0.15">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="{StaticResource Primary}" Offset="0" />
                            <GradientStop Color="Transparent" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- Logo -->
                <Frame x:Name="LogoImage" WidthRequest="64" HeightRequest="64" CornerRadius="32"
                       Background="{StaticResource Primary}" Padding="0"
                       HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="M" FontSize="28" FontAttributes="Bold" TextColor="White"
                           HorizontalOptions="Center" VerticalOptions="Center"/>
                </Frame>
            </Grid>

            <!-- App Name -->
            <VerticalStackLayout HorizontalOptions="Center" Spacing="4">
                <Label x:Name="AppNameLabel"
                       Text="MauMind"
                       FontSize="28"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"/>

                <!-- Tagline -->
                <Label x:Name="TaglineLabel"
                       Text="Your Private AI Assistant"
                       FontSize="14"
                       HorizontalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>
            </VerticalStackLayout>

            <!-- Loading -->
            <StackLayout x:Name="LoadingStack" 
                         HorizontalOptions="Center" 
                         Spacing="8"
                         Margin="0,24,0,0">
                <ActivityIndicator x:Name="LoadingIndicator"
                                   IsRunning="True"
                                   Color="{StaticResource Primary}"
                                   WidthRequest="20"
                                   HeightRequest="20"/>
            </StackLayout>

                 <!-- Version label -->
                 <Label x:Name="VersionLabel"
                     Text="v1.0"
                     FontSize="12"
                     Opacity="0.6"
                     HorizontalOptions="Center"
                     Margin="0,6,0,0"
                     TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>

        </VerticalStackLayout>

    </Grid>

</ContentPage>
