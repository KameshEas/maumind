<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    x:Class="MauMind.App.Views.ScanPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Scan"
    Background="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}">

    <Grid RowDefinitions="Auto,*">
        <!-- Premium Minimal Header -->
        <Grid Grid.Row="0" Margin="20,16,20,8" HeightRequest="56">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Back Button -->
            <Button Grid.Column="0" Text="&#x2190;" FontSize="18" 
                    BackgroundColor="Transparent" TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"
                    WidthRequest="36" HeightRequest="36" Padding="0"
                    Clicked="OnBackClicked"/>
            
            <!-- Title -->
            <StackLayout Grid.Column="1" VerticalOptions="Center" Margin="12,0">
                <Label Text="Scan Document" FontSize="18" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"/>
                <Label Text="Extract text from images" FontSize="12"
                       TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>
            </StackLayout>
        </Grid>

        <!-- Main Content -->
        <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Always" Padding="20,8">
            <StackLayout Spacing="20">
                
                <!-- Scan Options -->
                <StackLayout Spacing="12">
                    <Label Text="Choose Method" FontSize="14" FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"
                           Margin="4,0"/>

                    <!-- Camera Scan -->
                    <Frame CornerRadius="14" Padding="14"
                           Background="{AppThemeBinding Light={StaticResource LightSurface}, Dark={StaticResource DarkSurface}}"
                           HasShadow="False">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCameraScanTapped"/>
                        </Frame.GestureRecognizers>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Frame Grid.Column="0" WidthRequest="48" HeightRequest="48" CornerRadius="24"
                                   Background="{StaticResource Primary}" Padding="0">
                                <Label Text="&#x1F4F7;" FontSize="20" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Frame>
                            
                            <StackLayout Grid.Column="1" VerticalOptions="Center" Margin="14,0">
                                <Label Text="Camera" FontSize="15" FontAttributes="Bold"
                                       TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"/>
                                <Label Text="Take a photo" FontSize="12"
                                       TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>
                            </StackLayout>
                            
                            <Label Grid.Column="2" Text="&#x203A;" FontSize="18" VerticalOptions="Center"
                                   TextColor="{StaticResource Primary}"/>
                        </Grid>
                    </Frame>

                    <!-- Gallery Scan -->
                    <Frame CornerRadius="14" Padding="14"
                           Background="{AppThemeBinding Light={StaticResource LightSurface}, Dark={StaticResource DarkSurface}}"
                           HasShadow="False">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnGalleryScanTapped"/>
                        </Frame.GestureRecognizers>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Frame Grid.Column="0" WidthRequest="48" HeightRequest="48" CornerRadius="24"
                                   Background="{StaticResource Accent}" Padding="0">
                                <Label Text="&#x1F5BC;" FontSize="20" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Frame>
                            
                            <StackLayout Grid.Column="1" VerticalOptions="Center" Margin="14,0">
                                <Label Text="Gallery" FontSize="15" FontAttributes="Bold"
                                       TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"/>
                                <Label Text="Select image" FontSize="12"
                                       TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>
                            </StackLayout>
                            
                            <Label Grid.Column="2" Text="&#x203A;" FontSize="18" VerticalOptions="Center"
                                   TextColor="{StaticResource Accent}"/>
                        </Grid>
                    </Frame>
                </StackLayout>

                <!-- Loading Indicator -->
                <StackLayout x:Name="LoadingStack" IsVisible="False" Spacing="12" HorizontalOptions="Center" Margin="0,16">
                    <ActivityIndicator x:Name="LoadingIndicator" IsRunning="True" Color="{StaticResource Primary}" WidthRequest="32" HeightRequest="32"/>
                    <Label Text="Processing..." FontSize="14"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"/>
                </StackLayout>

                <!-- Results Section -->
                <StackLayout x:Name="ResultsStack" IsVisible="False" Spacing="12">
                    <Label Text="Extracted Text" FontSize="14" FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"/>

                    <!-- Extracted Text Display -->
                    <Frame CornerRadius="12" Padding="14" HeightRequest="200"
                           Background="{AppThemeBinding Light={StaticResource LightSurface}, Dark={StaticResource DarkSurface}}"
                           HasShadow="False">
                        <ScrollView>
                            <Editor x:Name="ExtractedTextEditor"
                                    FontSize="14"
                                    IsReadOnly="False"
                                    TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"
                                    BackgroundColor="Transparent"
                                    AutoSize="TextChanges"/>
                        </ScrollView>
                    </Frame>

                    <!-- Action Buttons -->
                    <StackLayout Orientation="Horizontal" Spacing="10" HorizontalOptions="Center" Margin="0,8,0,0">
                        <Button Text="Re-scan" 
                                Style="{StaticResource SecondaryButton}"
                                Clicked="OnRescanClicked"/>
                        <Button Text="Save" 
                                Style="{StaticResource PrimaryButton}"
                                Clicked="OnSaveDocumentClicked"/>
                    </StackLayout>
                </StackLayout>

                <!-- Tip Card -->
                <Frame CornerRadius="12" Padding="14"
                       Background="{AppThemeBinding Light={StaticResource PrimaryMuted}, Dark=#1A3A5C}"
                       HasShadow="False" Margin="0,12,0,0">
                    <StackLayout Spacing="6">
                        <Label Text="Tip" FontSize="13" FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"/>
                        <Label Text="Use clear images with good lighting for best results." FontSize="12" 
                               TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
                               LineBreakMode="WordWrap"/>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
